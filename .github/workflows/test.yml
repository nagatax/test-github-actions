name: Test
on:
    push

defaults:
    run:
        shell: bash

concurrency:
    group: ${{ github.workflow }}
    cancel-in-progress: true

jobs:
    print:
        strategy:
            matrix:
                lang: [php, go, python, node, java]
        runs-on: ubuntu-latest
        steps:
            - run: echo "${{ matrix.lang }}"