name: Manual
run-name: Run by @${{ github.actor }}
on:
    workflow_dispatch

jobs:
    run:
        name: Super Useful Job
        runs-on: ubuntu-latest
        steps:
            - run: |
                export RESULT="hello"
                echo "${RESULT}"
            - run: echo "${RESULT}"